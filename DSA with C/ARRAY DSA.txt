//*********INSERTION OPERATION ************
#include <stdio.h>
void take_input(int arr[], int n)
{
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &arr[i]);
    }
}
void insert_at(int arr[], int cap, int size, int index, int num)
{
    for (int i = size - 1; i >= index; i--)
    {
        arr[i + 1] = arr[i];
        // arr[i]=arr[i+1];
    }
    arr[index] = num;
}
void display(int arr[], int n)
{
    for (int i = 0; i < n; i++)
    {
        printf("\n%d\n", arr[i]);
    }
}
int main()
{
    int n, size, index = 2, number = 45;
    printf("ENTER THE CAPACITY AND SIZE OF AN ARRAY:");
    scanf("%d%d", &n, &size);
    int arr[n];

    take_input(arr, size);
    insert_at(arr, n, size, index, number);
    size += 1;
    display(arr, size);
    return 0;
}
//*********DELETION OPERATION ************
void take_input(int arr[], int size)
{
    printf("ENTER ARRAY ELEMENT:\n");
    for (int i = 0; i < size; i++)
    {
        scanf("%d", &arr[i]);
    }
}
void delete_at(int arr[], int size, int index)
{
    for (int i = index; i < size - 1; i++)
    {
        arr[i] = arr[i + 1];
    }
}
void display(int arr[], int size)
{
    printf("ARRAY AFTER DELETED THE ELEMENT:\n");
    for (int i = 0; i < size; i++)
    {
        printf("\n%d\n", arr[i]);
    }
}
int main()
{
    int n, size, index;
    printf("ENTER CAPACITY,SIZE AND INDEX WHICH YOU WANT TO DELETE:\n");
    scanf("%d%d%d", &n, &size, &index);
    int arr[n];
    take_input(arr, size);
    delete_at(arr, size, index);
    size -= 1;
    display(arr, size);
    return 0;
}

//*********LINEAR SEARCH************
#include <stdio.h>
void display(int arr[], int size)
{
    printf("ARRAY ELEMENTS:\n");
    for (int i = 0; i < size; i++)
    {
        printf("%d ", arr[i]);
    }
}
int linear_search(int arr[], int size, int element)
{
    for (int i = 0; i < size; i++)
    {
        if (arr[i] == element)
           return  printf("\nYES YOU ENTERED ELEMENT %d IS FOUND AT INDEX %d", arr[i], i);
    }
   return printf("ELEMENT IS NOT FOUND");
}
int main()
{
    int element;
    int arr[] = {1, 5, 4, 6, 9, 8, 9, 10, 15, 555};
    int size = sizeof(arr) / sizeof(int);
    display(arr, size);
    printf("\nENTER THE ELEMENT WHICH YOU WANT TO FIND :");
    scanf("%d", &element);
    linear_search(arr, size, element);
    return 0;
}
//*********BINAR SEARCH************
#include <stdio.h>
void display(int arr[], int size)
{
    printf("ARRAY ELEMENTS:\n");
    for (int i = 0; i < size; i++)
    {
        printf("%d ", arr[i]);
    }
}
int binary_search(int arr[], int size, int element)
{
    int low = 0;
    int high = size - 1;
    ONE IMPORTANT NOTE=LOW+(END-LOW)/2
    while (low <= high)
    {
        int mid = (low + high) / 2;
        if (arr[mid] == element)
        {
            return mid;
        }

        if (arr[mid] < element)
        {
            low = mid + 1;
        }
        else
        {
            high = mid - 1;
        }
    }

    return -1;
}
int main()
{
    int element;
    int arr[] = {1, 5, 9, 12, 15, 48, 77, 96, 100, 155};
    int size = sizeof(arr) / sizeof(int);
    display(arr, size);
    printf("\nENTER THE ELEMENT WHICH YOU WANT TO FIND :");
    scanf("%d", &element);
    int index = binary_search(arr, size, element);
    printf("ENTERED ELEMENT %d IS FOUND AT INDEX %d ", element, index);
    return 0;
}

//************OCCURANCE*************
#include <stdio.h>
void take_input(int arr[], int size)
{
    printf("ENTER 10 ARRAY ELEMENT :");
    for (int i = 0; i < size; i++)
    {
        scanf("%d", &arr[i]);
    }
}
void display(int arr[], int size)
{
    printf("\nARRAY ELEMENT :");
    for (int i = 0; i < size; i++)
    {
        printf("%d ", arr[i]);
    }
}
int first_occur(int arr[], int size, int element)
{
    int low = 0; int high = size - 1;int ans_store = -1;
    int mid = low + (high - low) / 2;
    while (low <= high)
    {
        mid = low + (high - low) / 2;
        if (arr[mid] == element)
        {
            ans_store = mid;
                high = mid - 1;
        }
        else if (arr[mid] < element)
        {
            low = mid + 1;
        }
        else
        {
            high = mid - 1;
        }
    }
    return ans_store;
}
int last_occur(int arr[], int size, int element)
{
    int low = 0;int high = size - 1;int ans_store = -1;
    int mid = low + (high - low) / 2;
    while (low <= high)
    {
        mid = low + (high - low) / 2;
        if (arr[mid] == element)
        {
            ans_store = mid;
                low = mid + 1;
        }
        else if (arr[mid] < element)
        {
            low = mid + 1;
        }
        else
        {
            high = mid - 1;
        }
    }
    return ans_store;
}
int main()
{
    int arr[20], element;
    take_input(arr, 10);
    display(arr, 10);
    printf("\nENTER ELEMENT WHICH YOU WANT TO FIND ITS FIRST AND LAST OCCURANCE :");
    scanf("%d", &element);
    int a=first_occur(arr, 10, element);
    int b=last_occur(arr, 10, element);
    printf("\nFIRST OCCURANCE OF %d IS %d",element,a);
    printf("\nLAST OCCURANCE OF %d IS %d",element,b);
}
//******************CREATED BY ME**************************
#include <stdio.h>
#include <stdlib.h>
int take_input(int arr[], int size)
{
    printf("\nENTER ARRAY ELEMENT:");
    int i;
    for (i = 0; i < size; i++)
    {
        scanf("%d", &arr[i]);
    }
    return arr[i];
}
void traversal(int arr[], int size)
{
    for (int i = 0; i < size; i++)
    {
        printf("\nELEMENT IS %d", arr[i]);
    }
}
int insert(int arr[],int size,int element){
    int i;
    for( i=size-1;i>=0;i--){
        arr[i+1]=arr[i];
    }
    return arr[i+1]=element;
}
int insert_index(int arr[], int size, int element, int index)
{
    int i;
    for (i = size - 1; i >= index; i--)
    {
        arr[i + 1] = arr[i];
    }
    return arr[index] = element;
}
int delete(int arr[], int size, int index)
{
    int i;
    for (i = index; i < size - 1; i++)
    {
        arr[i] = arr[i + 1];
    }
}
int main()
{
    int size, capacity;
    int element, index;
    printf("ENTER CAPACITY AND SIZE OF ARRAY:");
    scanf("%d%d", &capacity, &size);
    int arr[capacity];
    take_input(arr, size);
    traversal(arr, size);
    size +=1;
    printf("\nENTER ELEMENT WHICH YOU WANT TO INSERT :");
    scanf("%d", &element);
    insert(arr,size,element);

    printf("\nENTER INDEX WHERE YOU WANT TO INSERT :");
    scanf("%d", &index);
    insert_index(arr, size, element, index);

    printf("\nNEW ELEMENT IS INSERTED AT %d POSITION ", index);
    size += 1;
    traversal(arr, size);

    printf("\nENTER INDEX WHICH YOU WANT TO DELETE :");
    scanf("%d", &index);
    delete (arr, size, index);
    printf("\nELEMENT IS DELETED");
    size -= 1;
    traversal(arr, size);

    return 0;
}